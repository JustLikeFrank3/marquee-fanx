export type Fact = {event_id:number; field:string; claim:string; status:'supported'|'derived'; fetched_at:string; evidence_id:string};
export type Event = {event_id:number; performers:string[]; genres?:{performer:string;name:string;slug:string}[]; why?:string; budget_status?:'not_requested'|'unknown'|'within_estimate'; title:string; kind:string; date:string|null; time:string|null; date_tbd:boolean; time_tbd:boolean; timezone:string|null; venue_name:string; city:string; state:string; lowest_price:number|null; average_price:number|null; highest_price:number|null; listing_count:number|null; url:string|null; artwork_url:string|null; fetched_at:string; mode:'sample'|'live'; evidence_id:string; facts:Fact[]; seat_preview:{status:'external-link'|'unavailable';reason:string}};
export type Results = {events:Event[]; evidence_ids:string[]; statements:string; mode:'sample'|'live'; source:string; scope?:string; criteria?:Record<string,unknown>; cheapest_by_lowest_price?:number|null; coverage?:{exhaustive:boolean;candidates_considered:number;candidate_truncated:boolean}; genre_coverage?:{requested:string[];tagged_candidates:number;untagged_candidates:number;matching_candidates:number}; missing_favorites?:string[]; pricing_available?:boolean; related_discovery?:{status:string;source:string;searched_artists:{name:string;similar_to:string;match:number|null}[]}};
export type Report = {verdict:'PASS'|'FAIL'; claims_checked:number; facts:Fact[]; unsupported:{claim:string;reason:string;closest_supported:string|null}[]; report:string; limits:string; fetched_at:string};
export type Search = {query:string;genres?:string[];city:string;date_from:string;date_to:string;type:string;limit:number};
export type Tool = {name:string;description:string;inputSchema:Record<string,unknown>};
export type ToolCall = (name:string,args:Record<string,unknown>)=>Promise<unknown>;
